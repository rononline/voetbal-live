# Test / development dependencies for the Soccer Live integration.
# Reproducible local + CI test runs:  pip install -r requirements-test.txt
#
# Keep this lightweight — the unit/parser/config-flow tests use small Home
# Assistant stubs (no real HA needed), so they stay fast. The optional real
# Home Assistant integration test has its own heavier file:
#   pip install -r requirements-ha-test.txt
pytest==9.1.1
pytest-asyncio==1.4.0           # for the pytest.ini asyncio_mode=auto config (>=1.4 for the HA test plugin)
python-dateutil==2.9.0.post0    # runtime dependency, also imported directly by tests
PyYAML==6.0.3                   # scripts/validate_blueprints.py
ruff==0.16.6                    # critical static checks (undefined names/import errors)
